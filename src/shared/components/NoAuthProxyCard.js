// @ts-check
"use client";

/**
 * Proxy pools removed — free/no-auth providers no longer bind a pool.
 * Kept as a null component so existing imports do not break.
 */
export default function NoAuthProxyCard() {
  return null;
}
