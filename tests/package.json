{
  "name": "switchboard-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Unit tests for switchboard embeddings endpoint",
  "scripts": {
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest --reporter=verbose"
  },
  "devDependencies": {
    "happy-dom": "^20.11.6",
    "vitest": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
